---
topic: Agent Desk: Reflection por ciclo + controles PLAY (lock stop, meta, auto buy/sell)
updated: 2026-07-18T00:16
---

- (constraint) Reflection de agente só existe hoje para backtests (/api/backtest/{id}/reflection, /api/reflections); AgentDesk nao tem reflection por ciclo nem UI
- (constraint) _agent_loop_worker hardcode sl_pct=0.02 tp_pct=0.05 trailing_pct=0.01 e executa automatico sempre que verdict in (buy,sell) e conf>=0.5
- (constraint) PLAY/loop so funciona em modo REAL (_MODE==real e ALLOW_LIVE_TRADING=1); demo = so analise
- (capability) CAP-1: Reflection por ciclo de agente acessivel no frontend (endpoint + UI painel + historico)
- (capability) CAP-2: PLAY com controles configuraveis (SL%, TP%, Trailing%, Meta, Auto on/off, Lock Stop on/off)
- (capability) CAP-3: auto_trade=false faz PLAY virar so-analisa-e-avisa; execucao manual via botao Executar ordem existente
- (capability) CAP-4: lock_stop trava saida por stop-loss abaixo de piso definido; pode sair por meta/TP
- (capability) CAP-5: target_price (meta) define TP em preco absoluto, nao so %
- (decision) Escopo em 3 frentes: A=Reflection por ciclo, B=PLAY com controles, C=validacao (testes+build+server 200)
- (question) Lock stop: 'nao dispara SL abaixo de piso X' (trava saida so pra baixo) OU 'trava posicao inteira ate TP/meta'? (suspeita: nao sai por SL abaixo de X, mas sai por meta)
- (question) Meta (target): preco absoluto (ex 4.000) OU % de lucro (ex +10%) OU ambos?
- (question) auto buy/sell off = PLAY vira so-analisa-e-avisa e usuario executa manual pelo botao existente? Confirmar.
- (event) SELF-VALIDATE pass1: Spec Law 1-6,8 OK. 5 capabilities com intent+success (rule1). Intents sao WHAT (rule2). Constraints dobram design (rule3). Non-goals explicitos (rule4). Success signal concreto (rule5). IDs CAP-1..CAP-5 unicos/estaveis (rule6). Prosa lean (rule8).
- (event) SELF-VALIDATE pass2: Preservation OK. Toda claim load-bearing do diagnostico do usuario (reflection so backtest, hardcoded SL/TP, PLAY so REAL, 3 frentes) aterrou em Constraints/Capabilities. 3 open_questions do usuario (lock stop, meta, auto-off) preservadas em Open Questions, nao inventadas.

## [EVENT] 2026-07-18T00:48Z — IMPLEMENTADO (todos os CAPs)
- CAP-1: POST /api/agents/cycle/{cycle_id}/reflection + GET /api/agents/reflections (usa engine.get_closed_trades filtrado por cycle_id epoch-ms). Frontend: botão 🪞 Reflect + painel Reflection Agents.
- CAP-2: PLAY controls (SL%/TP%/Trailing%/Meta$/Auto/Lock) enviados em loop/start; worker respeita.
- CAP-3: auto_trade=false -> loop só analisa (log "analise-only"), não executa.
- CAP-4: lock_stop=true -> sl_pct=0 (sem SL duro; só sai na meta).
- CAP-5: target_price (absoluto $) convertido em tp_pct no entry.
- paper_engine: adicionado closed_trades (append no sell close) + get_closed_trades(since_ts).
- Testes: tests/unit/test_agent_loop_reflection.py (5 pass); vitest AgentDesk (2 pass). Full: 53 py + 9 fe pass. 3 falhas pré-existentes em test_agent_desk (confirmadas no HEAD limpo, não causadas por esta mudança).
- Servidor: todas rotas 200; endpoints novos validados (reflection retorna insights; loop/start aceita config; loop/status reflete config).
