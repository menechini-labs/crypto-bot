---
topic: agent desk → trade execution pipeline
updated: 2026-07-17T22:21
---

- (direction) user: analise que ainda nao estou conseguindo simular os trader por agents
- (event) audit: agent_desk.run_cycle() returns dict with agents[] + decision{} but NEVER calls PaperEngine.submit() or cli.run_cycle()
- (event) audit: cli.py run_cycle() uses _signal_for() from core/strategy.py directly, bypassing agent_desk entirely
- (event) audit: frontend AgentDesk.tsx renders verdict but has no 'Execute' button to send decision to PaperEngine
- (event) audit: endpoint GET /api/agents/cycle returns decision dict, but no POST /api/agents/execute to submit the decision as order
- (event) audit: two separate code paths exist — cli.py run_cycle() (standalone) and agent_desk.run_cycle() (API-facing); they never converge
- (event) audit: PaperEngine supports submit(symbol, side, qty) and runs fill/sl/tp/audit; the integration point exists but no bridge calls it from agent verdict
- (capability) CAP-1: AgentDesk.run_cycle() ou run_team() retorna decision{buy/sell/hold,confidence,reasoning} e submit automaticamente ao PaperEngine em REAL mode
- (capability) CAP-2: POST /api/agents/execute recebe agent_cycle_id e confirma/submete a ordem via PaperEngine; side=symbol, qty baseada em exposure/cash do snapshot
- (capability) CAP-3: cli.py run_cycle() substitui _signal_for() por agent_desk.run_cycle() e usa PaperEngine.submit() p/ executar ordens
- (capability) CAP-4: AgentDesk.tsx frontend mostra decision + botao 'Executar' → POST /api/agents/execute → ordem real/papeis
- (capability) CAP-5: historico de execucoes agentes (cycle_id → order_id → filled/expired) disponivel em /api/agents/history
- (constraint) DEMO mode: agent desk decide mas NUNCA executa (CAP-4 hidden, /api/agents/execute retorna 403)
- (constraint) qty calculada: usar snapshot().available_cash * confidence / last_price, respeitando LOT_SIZE minQty, 1 posicao por symbol
- (constraint) idempotencia: cycle_id unico, mesmo cycle_id rejeitado se ja executado
- (constraint) REAL mode exige ALLOW_LIVE_TRADING=1 igual resto do sistema; respeita _MAX_DAILY_ORDERS + _MAX_EXPOSURE_PCT
- (non-goal) NG-1: nao substituir PaperEngine — agent desk usa PaperEngine.submit() existente
- (non-goal) NG-2: nao adicionar LLM no AgentDesk — adiado para futuro LLM DecisionCore
- (non-goal) NG-3: nao criar machine learning on-chain — agent desk e heuristica pura
- (success) 1 clique AgentDesk → ordem paper filled com rastreamento audit
- (success) cli.py run_cycle() usando AgentDesk e PaperEngine — mesmo caminho do API
- (success) todos os 6 swarm presets executam ordens (crypto_trading_desk faz buy/sell, risk_committee so analise)
- (event) self-validate coherence pass: todas CAPs tem intent+success, constraints sao vinculantes, non-goals explicitos, signal testavel, lean prose
- (event) self-validate preservation pass: cada claim dos audit events mapeada p/ SPEC.md. sem wrapper-ceremony. sem open questions.
- (event) spec finalized: 5 capabilities, 8 constraints, 4 non-goals, 3 success criteria
